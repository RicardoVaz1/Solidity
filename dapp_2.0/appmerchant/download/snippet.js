// const snippetData = "Hello world";
// export default snippetData;


const snippetData = function payWithCrypto(amount) {
    // In the Checkout > Crypto RadioButton should add this: onClick={() => payWithCrypto(amount)})
    // And add this function in your server

    console.log("Amount: ", amount, "€")
}

export default snippetData